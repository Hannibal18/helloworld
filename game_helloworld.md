# 구현 요청: 바람의 나라 스타일 멀티플레이어 채팅 + PvP 웹게임

웹 브라우저에서 돌아가는 실시간 온라인 게임을 처음부터 끝까지 구현해줘. 접속한 사람들이 같은 맵을 돌아다니면서 말풍선으로 채팅하고, 서로를 때려서 HP를 깎는 PvP가 되는 게임이다. 아래 사양을 **그대로** 따른다. 임의로 기능을 늘리지 말고, 사양에 모호한 부분이 있으면 가장 단순한 쪽으로 구현해라.

---

## 1. 한 줄 목표

> 90년대 한국 MMORPG(바람의 나라) 감성의 탑다운 2D 도트 화면에서, 접속자끼리 실시간으로 돌아다니고 · 메이플스토리식 머리 위 말풍선으로 채팅하고 · 근접 공격으로 서로 패서 HP를 깎고 죽이는 게임.

기능은 딱 이 세 가지(이동/채팅/PvP)뿐이다. 로그인·회원가입·인벤토리·아이템·레벨 같은 건 전부 **만들지 마라.**

**PC(키보드/마우스)와 모바일(터치) 둘 다에서 동일하게 플레이 가능해야 한다.** 이건 선택이 아니라 필수 요구사항이다(§9 참고).

---

## 2. 기술 스택 (확정 — 변경 금지)

- **빌드/번들러**: Vite + **Vanilla TypeScript** (React/Vue 쓰지 마라. 캔버스 게임이라 프레임워크 불필요)
- **렌더링**: HTML5 Canvas 2D (외부 게임엔진·외부 이미지 에셋 금지. 캐릭터/타일은 전부 코드로 그린다)
- **실시간 네트워크**: **Supabase Realtime** (`@supabase/supabase-js` v2)
  - **Presence**: 누가 접속 중인지(아이디/이름/색) 추적, 입장/퇴장 처리
  - **Broadcast**: 위치/공격/채팅/HP 등 빠른 휘발성 메시지 전달
  - **DB·Auth·Edge Function은 쓰지 않는다.** (전투 판정은 아래 분산형 방식으로 처리)
- **폰트**: 한글 도트 폰트 (Galmuri 또는 NeoDunggeunmo) CDN 웹폰트. 폴백 monospace.

---

## 3. 핵심 설계 결정 (반드시 이대로)

### 3-1. 익명 입장
- 회원가입/로그인 없음. 입장 화면에서 **닉네임만** 입력하면 바로 접속.
- 플레이어 고유 id는 클라이언트에서 `crypto.randomUUID()`로 생성.
- 캐릭터 색은 입장 시 랜덤 배정(또는 간단한 색 선택 UI, 선택사항).

### 3-2. 권위 없는 분산형 전투 ⚠️ 가장 중요
Supabase Realtime에는 서버 로직이 없다(중계만 한다). 따라서 **각 클라이언트는 "자기 캐릭터"의 상태(위치·HP·방향)에 대해서만 권위를 가진다.**

전투 판정은 이렇게 한다:
1. 공격자는 공격 키를 누르면 **자기 화면에서 스윙 모션만 재생**하고, `attack` 이벤트를 broadcast 한다(자기 위치·방향 포함).
2. `attack`을 받은 **다른 모든 클라이언트는 "내 캐릭터"가 공격자의 히트박스 안에 있는지 스스로 계산**한다.
3. 맞았으면: **내가 내 HP를 깎고**, 넉백을 내 위치에 적용하고, 피격 이펙트(빨갛게 깜빡 + 짧은 무적시간)를 재생한 뒤, 바뀐 내 HP를 `hp` 이벤트로 broadcast 한다.
4. 내 HP가 0 이하가 되면 `death` 이벤트(killerId 포함)를 broadcast → 사망 연출 → 일정 시간 후 리스폰하며 위치·HP를 다시 broadcast.

→ 즉 "내가 남을 때린다"가 아니라 **"공격 신호를 보면 각자 자기가 맞았는지 판정한다"**. 이 구조 덕분에 중앙 서버가 필요 없다. (치팅이 가능하지만 캐주얼 게임이라 신경 안 쓴다. 코드 주석에 이 트레이드오프를 한 줄 남겨라.)

### 3-3. DB 불필요
Broadcast/Presence는 테이블도 RLS도 필요 없다. SQL 마이그레이션을 만들지 마라. (선택 확장인 킬 리더보드는 §13 참고 — 기본 구현에는 포함하지 않는다.)

---

## 4. Supabase 설정 (README에 그대로 적어줄 것)

사용자가 따라 할 수 있게 README에 단계를 명시:
1. supabase.com에서 새 프로젝트 생성.
2. Project Settings → API 에서 **Project URL**과 **anon public key** 복사.
3. `.env` 파일에 넣기 (Vite는 `VITE_` 접두사 필요):
   ```
   VITE_SUPABASE_URL=https://xxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGci...
   ```
4. Realtime은 신규 프로젝트에서 기본 활성화 → **Broadcast/Presence는 추가 설정 없이 바로 동작**한다.
5. `npm install && npm run dev` 후 브라우저 탭 2개로 접속해 테스트.
6. anon key는 클라이언트에 노출돼도 안전한 공개 키임을 README에 명시. `.env`는 `.gitignore`에 포함, `.env.example` 제공.

Supabase 채널 초기화는 이 형태를 기준으로:
```ts
const channel = supabase.channel('baram-room', {
  config: {
    broadcast: { self: false },      // 내 메시지는 내가 안 받음 (중복 처리 방지)
    presence: { key: myId },
  },
});

channel
  .on('broadcast', { event: 'pos' },    ({ payload }) => onPos(payload))
  .on('broadcast', { event: 'chat' },   ({ payload }) => onChat(payload))
  .on('broadcast', { event: 'attack' }, ({ payload }) => onAttack(payload))
  .on('broadcast', { event: 'hp' },     ({ payload }) => onHp(payload))
  .on('broadcast', { event: 'death' },  ({ payload }) => onDeath(payload))
  .on('presence', { event: 'sync' },    () => syncRoster(channel.presenceState()))
  .on('presence', { event: 'join' },    ({ newPresences }) => {/* 신규 입장 → 내 위치 즉시 1회 재전송 */})
  .on('presence', { event: 'leave' },   ({ leftPresences }) => removePlayers(leftPresences))
  .subscribe(async (status) => {
    if (status === 'SUBSCRIBED') {
      await channel.track({ id: myId, name, color });
    }
  });
```

---

## 5. 프로젝트 구조

```
baram-pvp/
  index.html
  package.json
  vite.config.ts
  tsconfig.json
  .env.example
  .gitignore
  README.md
  src/
    main.ts        # 부트스트랩: 입장 화면 → 게임 시작
    supabase.ts    # Supabase 클라이언트 + 채널 셋업
    net.ts         # 송수신 래퍼(타입 안전한 broadcast/presence)
    types.ts       # 공유 타입 + 네트워크 페이로드 정의
    game.ts        # 게임 루프(requestAnimationFrame), 전역 상태, 업데이트
    player.ts      # 로컬 플레이어: 입력·이동·충돌·공격 히트박스·피격
    input.ts       # 입력 추상화: 키보드/마우스 + 터치를 공통 inputState로 통합
    controls.ts    # 모바일 가상 컨트롤(조이스틱·공격/채팅 버튼) 오버레이
    world.ts       # 타일맵 생성 + 충돌 + 카메라
    sprites.ts     # 캐릭터/타일 도트를 코드로 그리는 함수
    render.ts      # 한 프레임 렌더: 맵 → 원격 플레이어 → 로컬 → 말풍선/HP바/이름
    ui.ts          # 입장 화면, 채팅 입력창, 접속자 수 표시
    style.css
```

---

## 6. 네트워크 프로토콜 (이대로 구현)

모든 페이로드에 보낸 사람 `id` 포함. 전부 broadcast(아래 표), 신원만 presence.

| 이벤트 | 발신 시점 | 페이로드 | 수신 측 처리 |
|---|---|---|---|
| presence `track` | 입장 시 / 갱신 | `{ id, name, color }` | 로스터(원격 플레이어 목록)에 추가/갱신 |
| `pos` | 이동 중 ~10회/초 throttle, 정지 시 1회, 유휴 시 1초마다 heartbeat | `{ id, x, y, dir, moving }` | 해당 원격 플레이어 좌표/방향/걷기상태 갱신(보간) |
| `chat` | 채팅 전송 시 | `{ id, text }` (text ≤ 100자) | 해당 캐릭터 머리 위 말풍선 표시(약 4초) |
| `attack` | 공격 키 입력 시 | `{ id, x, y, dir }` | **내 캐릭터가 히트박스 안인지 스스로 판정** → 맞으면 자기 피해/넉백 적용 |
| `hp` | 내 HP 변동 시 | `{ id, hp }` | 해당 캐릭터 HP바 갱신 |
| `death` | 내 HP ≤ 0 | `{ id, killerId }` | 사망 연출. killerId가 나면 내 킬 +1(선택) |
| (리스폰) | 부활 시 | `pos` + `hp` 재전송 | 위치/HP 복구 |

**지각 입장(late join) 처리**: 모든 클라이언트가 유휴 시에도 1초 heartbeat로 `pos`를 쏘므로, 새 접속자는 ~1초 안에 모두를 본다. 추가로 presence `join` 수신 시 기존 클라이언트는 자기 `pos`를 즉시 1회 더 쏜다.

---

## 7. 게임플레이 사양

### 이동
- **PC 키**: 방향키 **그리고** WASD 둘 다 지원. (모바일 조작은 §9)
- 4방향(상하좌우) 이동, 대각선은 양축 동시 입력 허용.
- 로컬 플레이어는 **즉시 로컬에서 움직이고**(예측), 원격 플레이어는 받은 `pos`로 부드럽게 보간(lerp).
- 월드 경계 및 충돌 타일(나무·바위·벽)에서 막힘.
- 이동 속도: 약 120px/초 기준(타일 32px). 체감 보고 조정 가능.

### 전투 (수치는 시작값, 밸런스는 체감 후 미세조정 허용)
- 최대 HP: **100**
- 공격 키: **Space** 또는 마우스 클릭 (모바일은 화면 공격 버튼, §9)
- 공격 쿨다운: **0.5초**
- 히트박스: 바라보는 방향 앞쪽 **약 40px 길이 × 36px 폭**의 사각형
- 데미지: **20**
- 넉백: 피격 시 공격 방향으로 **약 24px** 밀림
- 무적시간(i-frame): 피격 후 **0.4초** (이 동안 추가 피격 무시)
- 공격 모션: 짧은 스윙 프레임(무기 휘두르거나 주먹 내지르는 1~2프레임)
- 피격 모션: 캐릭터 빨갛게 깜빡 + 데미지 숫자가 머리 위로 살짝 떠오르며 사라짐(선택이지만 권장)

### 사망 / 리스폰
- HP ≤ 0 → 사망 연출(쓰러지거나 반투명+회전 등 간단히) → `death` broadcast.
- **3초 후** 랜덤 스폰 지점에서 부활, HP 100으로 복구, 위치·HP 재전송.
- (선택) 킬 카운트: 내가 보낸 `death`의 killerId가 어떤 플레이어면 그 플레이어 화면에서 자기 킬 +1, 이름 옆에 표시.

### 채팅 (메이플스토리식 말풍선)
- **PC**: **Enter**로 채팅 입력창 포커스 → 타이핑 → **Enter**로 전송, **Esc**로 취소.
- **모바일**: 화면의 채팅 버튼/입력칸을 탭하면 입력창 포커스(OS 키보드 올라옴) → 전송 버튼 또는 키보드 확인으로 전송.
- 입력창 포커스 중에는 이동/공격 키가 게임에 안 먹히게 분리(키 입력은 텍스트로).
- 전송하면 내 캐릭터 머리 위에 말풍선이 뜨고 `chat` broadcast → 모두의 화면 해당 캐릭터 위에 동일 말풍선.
- 말풍선: 흰 배경 둥근 사각형 + 아래쪽 꼬리, 검은 도트 글씨, 긴 글은 자동 줄바꿈, **약 4초 후 사라짐**. 새 말풍선 오면 교체.

---

## 8. 비주얼 사양 (바람의 나라 룩)

- **탑다운 직교 타일맵**, 타일 32px. 흙/풀/돌길 등 90년대풍 흙빛 팔레트. 약간의 텍스처 변주(같은 풀 타일도 미세하게 다르게)로 단조로움 제거.
- 월드 크기 예: 약 **60×40 타일**. 나무·바위·작은 마을 구조물 같은 랜드마크 + 충돌 오브젝트 배치.
- **카메라**는 로컬 플레이어를 따라가고 월드 경계에서 클램프.
- **캐릭터(도트, 코드로 그림)**: 약 24×32px 휴머노이드. 몸통(로브=플레이어 색)·머리카락·**4방향 페이싱**·걷기 2프레임 바운스·공격 스윙 프레임. 가독성 위해 외곽선.
- 캐릭터 위 표시: **이름(도트 폰트)** → 그 아래 **HP바(현재/최대 비율)** → (선택) 킬 수.
- **픽셀 선명도 필수**: CSS `image-rendering: pixelated`, 캔버스 정수 배율 스케일링, `ctx.imageSmoothingEnabled = false`. 저해상도 내부 캔버스를 정수배로 확대하는 방식 권장.
- 전반적으로 도트 + 둥근모 폰트로 향수 자극하는 톤. 화려한 그라데이션·이모지 금지.

---

## 9. 크로스플랫폼 입력 & 반응형 (PC + 모바일, 필수)

같은 코드 한 벌이 데스크톱과 모바일 브라우저에서 모두 돌아가야 한다. 별도 앱/별도 빌드를 만들지 말고, **입력 방식만 기기에 따라 분기**한다.

### 9-1. 입력 분기
- 터치 지원 여부를 감지(`'ontouchstart' in window` 또는 포인터 타입)해서 **모바일이면 화면 가상 컨트롤을 표시**, PC면 숨김. 둘 다 가능한 환경(터치 노트북)은 마지막 입력 종류에 따라 부드럽게 전환되면 좋지만 필수는 아님.
- 같은 게임 상태(이동 벡터·공격 트리거)에 PC 입력과 터치 입력이 **동일하게 흘러들어가도록** 입력 레이어를 추상화한다(키/터치가 같은 `inputState`를 채움).

### 9-2. 모바일 가상 컨트롤 (화면 오버레이)
- **왼쪽 아래: 가상 조이스틱(또는 D-패드)** — 드래그 방향으로 이동, 손 떼면 정지. 조이스틱은 8방향 정도 허용(렌더는 4방향 스프라이트로 매핑).
- **오른쪽 아래: 공격 버튼** — 탭하면 공격(쿨다운은 PC와 동일).
- **채팅 버튼**(예: 오른쪽 위 또는 공격 옆 작은 버튼) — 탭하면 채팅 입력 활성.
- 컨트롤은 반투명 도트 스타일로, 게임 화면을 너무 가리지 않게. 안전영역(노치/홈바) 고려: `env(safe-area-inset-*)` 패딩.

### 9-3. 반응형 화면 / 픽셀 선명도
- `index.html`에 모바일 뷰포트 메타: `<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">`.
- 캔버스는 **저해상도 내부 버퍼 + 화면 크기에 맞춰 스케일**. 내부 해상도(논리 픽셀)는 고정/적당히 두고 CSS로 화면에 꽉 차게 확대하되, **`image-rendering: pixelated` + `imageSmoothingEnabled=false`로 도트가 또렷하게** 유지. 고DPR(레티나) 기기에서 흐려지지 않도록 `devicePixelRatio`를 반영해 백버퍼 크기를 잡되, 픽셀아트 룩은 깨지지 않게.
- 화면이 좁은 모바일에서는 **카메라 줌(논리 뷰포트 타일 수)을 PC보다 약간 더 크게** 잡아 캐릭터가 적당한 크기로 보이게(예: PC는 가로 ~20타일, 모바일 세로모드는 ~12타일). 리사이즈/회전(`resize`, `orientationchange`) 시 재계산.

### 9-4. 터치 기본동작 차단
- 게임 영역에 `touch-action: none`, 더블탭 줌·당겨서 새로고침·핀치줌·스크롤 방지(필요한 `preventDefault`). 단 채팅 입력칸은 정상적으로 포커스/타이핑 가능해야 한다.
- 모바일 OS 키보드가 올라올 때 레이아웃이 깨지지 않게(입력창은 오버레이로 띄우고, 키보드가 가리면 입력창을 위로 올림).

### 9-5. 주의
- 모바일 성능 고려: 위치 broadcast throttle·렌더 부하를 과하게 키우지 마라. 60fps 목표, 안 되면 30fps라도 안정적으로.
- 터치 멀티포인트(왼손 이동 + 오른손 공격 동시)가 동작해야 한다 — 조이스틱 드래그 중에도 공격 버튼이 먹혀야 함. `touchstart/touchmove/touchend`에서 `identifier`로 손가락 구분.

---

## 10. UI / 온보딩

1. **입장 화면**: 게임 타이틀(도트 폰트) + 닉네임 입력 + "입장" 버튼. (색 선택은 선택사항)
2. **게임 화면**: 캔버스 풀스크린, 좌상단에 **현재 접속자 수**, 하단에 채팅 입력창(평소엔 흐릿하다가 활성 시 또렷). PC는 우상단(선택)에 조작 안내(이동: WASD/방향키, 공격: Space, 채팅: Enter). **모바일은 §9의 가상 조이스틱·공격/채팅 버튼 오버레이**가 보인다.
3. 페이지 종료(`beforeunload`)·탭 닫힘 시 `channel.unsubscribe()`로 presence 정리(다른 사람 화면에서 내 캐릭터 사라지게).

---

## 11. 구현 시 주의사항 (함정 미리 차단)

- `broadcast: { self: false }` 로 **자기 메시지 자기가 안 받게** 해서 중복 처리 방지.
- 위치 broadcast는 **반드시 throttle**(이동 중 ~10/s). Realtime 무료 티어 메시지 레이트 한도가 있으니 매 프레임 쏘지 마라.
- 좌표는 **월드 좌표로 송수신**, 렌더링만 카메라 기준 상대좌표로 변환.
- 채팅 포커스 중 키 입력과 게임 키 입력을 **확실히 분리**(채팅 중엔 캐릭터 안 움직이게).
- 무적시간 동안 중복 피격 무시 처리 빼먹지 마라(안 그러면 1번 공격에 여러 번 깎임).
- 원격 플레이어 위치는 보간(lerp)으로 부드럽게. 순간이동처럼 튀지 않게.
- 퇴장/리스폰/지각입장 엣지케이스(§6) 처리.
- 타입은 `types.ts`에 모든 페이로드를 정의하고 송수신 양쪽에서 공유.
- 코드는 모듈별로 깔끔히 분리하고 핵심 로직(특히 분산형 전투 판정)에 주석.

---

## 12. 완료 기준 (이 체크리스트가 전부 통과해야 끝)

브라우저 탭 2~3개로 같은 방에 접속해서 아래를 직접 확인:

- [ ] 닉네임 입력 후 입장하면 같은 맵에 캐릭터가 생긴다.
- [ ] A 탭에서 움직이면 B 탭에서 그 캐릭터가 부드럽게 따라 움직인다(끊김/순간이동 없음).
- [ ] 채팅을 보내면 보낸 캐릭터 머리 위에 말풍선이 뜨고, 모든 탭에서 동일하게 보이며 ~4초 후 사라진다.
- [ ] A가 B 앞에서 공격하면 B의 HP가 줄고(빨강 깜빡 + 넉백), 모든 탭의 B HP바가 같이 줄어든다.
- [ ] HP가 0이 되면 사망 연출 후 3초 뒤 다른 위치에서 HP 100으로 부활한다.
- [ ] 좌상단 접속자 수가 입장/퇴장에 맞게 갱신된다.
- [ ] 늦게 들어온 탭도 1초 안에 기존 접속자 전원을 본다.
- [ ] 탭을 닫으면 다른 탭에서 그 캐릭터가 사라진다.
- [ ] 픽셀이 또렷하고(블러 없음) 둥근모 폰트가 적용돼 바람의 나라 느낌이 난다.
- [ ] **PC(키보드/마우스)와 모바일(터치) 둘 다에서 플레이된다.** 핸드폰 브라우저에서 가상 조이스틱으로 이동, 공격 버튼으로 공격, 채팅 버튼으로 채팅이 모두 된다.
- [ ] 모바일에서 **이동 조이스틱과 공격 버튼을 동시에** 눌러도 둘 다 작동한다(멀티터치).
- [ ] 핸드폰과 PC가 **같은 방에서 함께** 보이고 서로 때릴 수 있다.
- [ ] 모바일 세로/가로 회전 시 화면이 깨지지 않고, 핀치줌·당겨새로고침이 게임 중 발생하지 않는다.

`README.md`에 위 셋업(§4) + 실행 + 배포(정적 호스팅: Vercel/Netlify/Cloudflare Pages 중 하나 예시) 방법을 적어줘.

---

## 13. (선택) 확장 — 킬 리더보드 *기본 구현엔 넣지 마라*

나중에 원하면 추가할 수 있게 README 하단에 메모만:
- Supabase에 `kills (id, name, kills, updated_at)` 테이블 + RLS 정책을 두고, `death` 발생 시 killer가 자기 카운트를 upsert, Postgres Changes나 단순 폴링으로 화면 우측에 TOP 5 표시. **단, 지금은 만들지 말 것.**
